apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kargo
  namespace: kargo
spec:
  rules:
  - host: kargo.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: kargo-api
            port:
              number: 80