apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-baseline
  namespace: redis
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
  ingress:
    - {}
  egress:
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 53
        - protocol: UDP
          port: 53