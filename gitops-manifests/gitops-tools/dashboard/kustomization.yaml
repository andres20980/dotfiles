apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dashboard

resources:
  - admin-rbac.yaml
  - deployment.yaml
  - metrics-scraper.yaml
  - rbac.yaml
  - secret.yaml
  - service.yaml
  - networkpolicy.yaml
  # Ingress deshabilitado: Kind usa NodePort (no ingress controller)
  # - ingress.yaml
  # PDB deshabilitado: innecesario en Kind single-node (best-practice para dev)
  # - pdb.yaml
