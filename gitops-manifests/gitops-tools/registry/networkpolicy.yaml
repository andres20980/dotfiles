apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: registry-baseline
  namespace: registry
spec:
  podSelector: {}
  policyTypes: [Ingress, Egress]
  ingress:
    - {}
  egress:
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: TCP
          port: 53
        - protocol: UDP
          port: 53