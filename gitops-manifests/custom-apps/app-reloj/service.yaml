apiVersion: v1
kind: Service
metadata:
  name: app-reloj
  labels:
    app: app-reloj
spec:
  type: NodePort
  selector:
    app: app-reloj
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30150
      protocol: TCP
