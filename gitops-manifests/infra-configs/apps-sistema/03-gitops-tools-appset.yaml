# ApplicationSet para herramientas GitOps - List Generator explícito (best-practice para repos simples)
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gitops-tools
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  generators:
    - list:
        elements:
          - app: dashboard
            project: gitops-tools
          - app: grafana
            project: gitops-tools
          - app: kargo
            project: gitops-tools
          - app: kargo-crds
            project: gitops-tools
          - app: prometheus
            project: gitops-tools
          - app: redis
            project: gitops-tools
          - app: registry
            project: gitops-tools
  template:
    metadata:
      name: '{{app}}'
      namespace: argocd
      finalizers:
        - resources-finalizer.argocd.argoproj.io
      annotations:
        # Forzar refresh inmediato tras creación
        argocd.argoproj.io/refresh: hard
    spec:
      project: '{{project}}'
      source:
        repoURL: http://gitea.gitea.svc.cluster.local:3000/gitops/gitops-manifests.git
        targetRevision: main
        path: 'gitops-tools/{{app}}'
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{app}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            maxDuration: 3m
