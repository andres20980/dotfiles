apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  # Wave 0: ArgoCD self-management
  - 00-argocd-self.yaml
  
  # Wave 1: Sealed Secrets (prerequisito para secretos)
  # - 01-sealed-secrets.yaml
  
  # Wave 2: Gitea (source of truth)
  - 02-gitea.yaml
  
  # Wave 3: Argo Tools ApplicationSet (argo-workflows, argo-events, argo-rollouts)
  - 03-argo-tools-appset.yaml
  
  # Wave 3: GitOps Tools ApplicationSet (dashboard, grafana, kargo, etc.)
  - 03-gitops-tools-appset.yaml
  
  # Wave 4: Custom Apps ApplicationSet
  - 04-custom-apps-appset.yaml
