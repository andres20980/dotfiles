# Docker Registry - Almacenamiento interno de imágenes
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: docker-registry
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    # Durante bootstrap: usa GitHub
    # Después de migración: apuntará a Gitea
    repoURL: https://github.com/andres20980/dotfiles.git
    targetRevision: main
    path: gitops-manifests/gitops-tools/registry
  destination:
    server: https://kubernetes.default.svc
    namespace: registry
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        maxDuration: 3m
